{
  "name": "@teamest/internal-season-contract",
  "version": "0.3.1",
  "description": "Contract and generator for the internal season service",
  "main": "index.js",
  "scripts": {
    "generate": "npx generate-rpc ./config/InternalSeason.specification.json",
    "build-client": "cd client && npm i && npm run build",
    "build-server": "cd server && npm i && npm run build",
    "clean": "rm -rf client && rm -rf server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/teamest-au/internal-season-contract.git"
  },
  "author": "Daniel Emery <danielremery@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/teamest-au/internal-season-contract/issues"
  },
  "homepage": "https://github.com/teamest-au/internal-season-contract#readme",
  "dependencies": {
    "@teamest/models": "^0.9.2"
  },
  "devDependencies": {
    "@danielemeryau/dumb-node-rpc": "^0.1.1",
    "@danielemeryau/prettier-config": "^0.0.6"
  }
}
